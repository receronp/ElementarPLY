programm
    // Declaration of variables.
    dim tempA, tempB als word
    dim A, B, C, D, E, F, G, H, I, J als word

begin
    tempA = A * B + C * ( D + E * ( F * G + H ) + I * J ) * K + L
    tempB = (A + B) * C + ( D + E ) * ( F + G * ( H * I + J) * K + L) 

schluss